# バグ調査レポート: テックツリーのノードクリック無反応 + ミニマップ白箱

> **issue / slug**: C20260622-001 / node-click-minimap
> **重大度**: high
> **再現可否**: 可（ハンドラ/テーマ未実装のため確定的）
> **報告日**: 2026-06-22
> **起点クレーム**: ../claim_C20260622-001_20260622_node-click-minimap/001_TRIAGE.md

## 1. 症状
- (A) テックツリー右下のミニマップが白い箱で、全体俯瞰が判別できない。
- (B) テックツリーのノード（単元）をクリックしても何も起きず、学習画面に遷移しない。

## 2. 期待挙動
- concept §1.1 UC#1（ミニマップで全体俯瞰）/ UC#3（ノード選択→学習）、tech-tree SPEC §6.1
  「ノード選択 → 詳細パネル（豆知識）→ 解放済みなら「学ぶ」で learning-workbook へ / 未解放は導線無効 / ミニマップで全体表示」。

## 3. 再現手順
1. https://math-tree.givers.work/ を開く。
2. (B) 任意のノード（例「正負の数の計算」）をクリック → 無反応。
3. (A) 右下のミニマップを見る → 白い箱で内容が判別できない。

## 4. 再現環境
| 項目 | 値 |
|---|---|
| URL | 本番 math-tree.givers.work |
| 画面幅 | モバイル幅で確認（全幅で同様） |
| 再現性 | 常時 |

## 5. 影響範囲
- 影響ユーザー: 全ユーザー。
- 影響機能: **核心の体験ループ起点（テックツリー→単元学習）が機能しない**。
- データ影響: なし。セキュリティ影響: なし。
- 注記: 公開直後・実ユーザー流入前（自己検証で発見）。

## 6. 暫定回避策
URL 直打ち（`/learn/<slug>`）でのみ学習画面に到達可能（一般ユーザーには非現実的）。

## 7. 関連ログ・スタックトレース
なし（例外でなく機能未配線）。

## 8. 過去類似バグ
該当なし（本 PJ 初の fix）。関連: 直前の `daac031` で縦レイアウト化、`800ffae` で screen スタイル適用（その際 MiniMap の mask/nodeColor 未テーマ化が残存）。

## 9. 関連 AI_LOG タイムライン
- 設計起点: D20260620_016_feature_tech-tree（SPEC §6.1 にノード選択→学習フロー記載）。
- 実装: D20260620_030_tdd_tech-tree（描画・状態配色は実装、**onNodeClick 配線は未実装のまま**）。
- 直近: D20260621_043_fix（縦レイアウト）/ D20260622_048_design（screen スタイル適用、MiniMap テーマ未完）。
- 起点クレーム: D20260622_049_claim（バグ判定）。

## 10. タイムラインから抽出した仮説
- 仮説1: tdd 実装時にノード描画は作ったが **onNodeClick → /learn 遷移の配線が漏れた**（O57 系「部品は在るが配線が無い」）。
- 仮説2: MiniMap は既定コンポーネントを置いただけで mask/nodeColor のテーマ適用が漏れた。
