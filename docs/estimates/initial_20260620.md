---
generated_at: 2026-06-20 09:05
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md
interpreted_as:
  feature: null
  phase: rough
  source: autodetect
phase: rough
confidence_band:
  ai_impl: "±300%"
  human_bottleneck: "±50%"
  ai_tokens: "±30% (設計) / ±50% (実装込み)"
nfr_profile:
  scale: low
  throughput: low
  latency: standard
  availability: 99
  source: default
calibration:
  global_metrics: "global-metrics.jsonl 空（実績0）→ 内蔵典型値 100%"
  pj_stats: "STATS.md なし（新規 PJ）"
  confidence_note: "N<5 のため rough band（±300%）を維持"
summary:
  min:
    total_files: 40
    total_lines: 4200
    human_hours: 8
    ai_tokens_k: 100
  standard:
    total_files: 95
    total_lines: 10500
    human_hours: 16
    ai_tokens_k: 224
  full:
    total_files: 190
    total_lines: 21000
    human_hours: 32
    ai_tokens_k: 388
---

# 見積もり: Math-Tree 全体（初回 rough）

> Phase 1 完了直後の初回フェルミ推定。concept §1.3（機能5 + 横断8）+ §3 NFR から算出。
> **時間・金額は主指標にしない** — コード量 / 人間時間 / AI 推論トークン量が主指標。

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 40 | 4,200 | 8h | 設計 90K / 実装 10K = 合計 100K (±50%) |
| Standard | 95 | 10,500 | 16h | 設計 200K / 実装 24K = 合計 224K (±50%) |
| Full | 190 | 21,000 | 32h | 設計 340K / 実装 48K = 合計 388K (±50%) |

> **confidence band**: AI-impl ±300%（phase=rough、concept レベル・SPEC 未生成）/ Human-bottleneck ±50% / AI 推論トークン ±30%(設計)・±50%(実装込み)
> **NFR プロファイル**: scale=low, throughput=low, latency=standard, availability=99（**default 想定値** — 個人ツール DAU〜100、§3 NFR 由来。NFR 倍率 ≈ 0.7×0.8×1.0×1.0 = 0.56x を反映済み）
> **金額換算**: コード行数 × AI コーディングレート、トークン × モデル単価（その時期で要確認）で利用側が算出。

## 1. 機能フォルダ別ブレークダウン（Standard 基準、5 機能）

| フォルダ | 主要 task_type | classification | Std files | Std lines | 備考 |
|---|---|---|---|---|---|
| tech-tree | ui_code / api_code / e2e_auto | ai_impl | 8 | 1,300 | React Flow カスタムノード/エッジ + 周辺フォーカス + ミニマップ + アンロック進捗オーバーレイ + ノード詳細(豆知識) + progress API |
| learning-workbook | ui_code / api_code / unit_test | ai_impl | 10 | 1,600 | MathLive エディタ + ステップ UI + **CAS 同値判定([論点-002])** + AI フォールバック照合 + 習得判定 API。複雑度高 |
| curriculum-generation | api_code / unit_test | ai_impl | 10 | 1,450 | AI 生成オーケストレーション + **多段クロス検証パイプライン([論点-001])** + キャッシュ配信 + コンテンツ承認 + 生成バッチ。複雑度高 |
| feedback | ui_code / api_code | ai_impl | 6 | 700 | ウィジェット + ingestion + **PII scrub(SEC-003)** + feedback-hub 連携 |
| support | ui_code / api_code | ai_impl | 5 | 550 | tip-jar ボタン + Stripe Checkout + Webhook(署名検証) |
| **機能小計** | | | **39** | **5,600** | |

## 2. 横断フォルダ別ブレークダウン（Standard 基準、8 横断、全て is_new=true）

| フォルダ | classification | Std files | Std lines | 備考 |
|---|---|---|---|---|
| _shared/db | ai_impl | 6 | 600 | 約10テーブル(単元/エッジ/問題/ステップ/進捗/支援/feedback/コストログ/検証) + Drizzle + マイグレーション |
| _shared/ui | ai_impl | 8 | 750 | shadcn テーマトークン + 基本コンポーネント + KaTeX ラッパ + 自作 SVG |
| _shared/auth | ai_impl | 6 | 700 | Clerk 匿名ゲスト + 段階認証 + **owner resolver(SEC-001)** + データ引き継ぎ |
| _shared/app-shell | ai_impl | 8 | 700 | 合成ルート + router + providers + API ルートハンドラ層 + deploy scaffold |
| _shared/ai | ai_impl | 5 | 600 | Claude クライアント(キー秘匿) + レート + 呼び出しログ |
| _shared/cost-tracking | ai_impl | 5 | 500 | 呼び出しログ積算 + .env 単価 + 概算コスト + 無料枠アラート |
| _shared/types | ai_impl | 4 | 250 | 単元/エッジ/進捗/問題/ステップ/豆知識 型 |
| _shared/legal | ai_impl (doc 多) | 4 | 250 | プラポリ/利用規約/特商法 + consent。**DSR セルフ削除導線(SEC-004)** |
| **横断小計** | | **46** | **4,350** | |

## 3. 基本部分（12 項目、whole のみ）

| 項目 | Std files | Std lines | 備考 |
|---|---|---|---|
| プロジェクト初期化 / CI/CD / 監視(Sentry) / 分析(Vercel WA) / dev スクリプト 等 | 10 | 500 | 大半は横断フォルダに吸収。残りの初期化・GitHub Actions・dev.sh/stop.sh を計上 |
| **基本小計** | **10** | **500** | |

## 4. NFR 倍率の効き

| NFR 軸 | 値 | 倍率 | 影響 |
|---|---|---|---|
| scale | low | 0.7x | DAU〜100、シャーディング不要 |
| throughput | low | 0.8x | 事前生成キャッシュ配信主体 |
| latency | standard | 1.0x | — |
| availability | 99 | 1.0x | 個人ツール水準 |

合計 NFR 倍率: **≈ 0.56x**（上記ブレークダウンに反映済み）

## 5. 人間時間（Human-bottleneck、±50%）

| 項目 | classification | Std hours | 備考 |
|---|---|---|---|
| [論点-001/002] 設計判断の最終確認 | human_bottleneck | 3h | AI 検証パイプライン段数・合格基準 / 数式同値判定方式の承認 |
| デザインレビュー承認（/flow:design） | human_bottleneck | 2h | 視覚レビュー green 承認 |
| wording 校正（/flow:wording） | human_bottleneck | 3h | UI 文言の声を人間判断 |
| release（実機スモーク + Stripe live + デプロイ確認） | human_bottleneck | 4h | tip-jar 実課金 1 回確認 + モバイル確認 + Class B デプロイ承認 |
| 法務文書 最終確認 | human_bottleneck | 2h | プラポリ/利用規約/特商法 公開前確認 |
| AI 生成コンテンツの体験確認（数学は AI 検証に委譲） | human_bottleneck | 2h | 縦スライス 1 系統の通し確認 |
| **小計** | | **16h** | E2E は自動化基本（Playwright 等）のため計上外 |

## 6. AI 推論トークン量（フェルミ推定）

**設計フェーズ（Std ~200K）**: concept NEW 13K + secure design 11K + estimate 7K + design 12K + feature ×13（10K）= 130K + audit light×2 26K + audit full 1回 33K + secure pre-impl/deps 14K。
**実装フェーズ（Std ~24K）**: 10,500 lines × 1.5 tokens/line × loop 1.5 ≈ 24K（TDD リトライ込み）。

> 単価は時期変動のため参考併記のみ（2026-06 時点想定、要確認）。トークン数が主指標。

## 7. 二重計上回避
全 8 横断フォルダは `is_new=true`（新規 PJ、既存設計なし）。二重計上なし。

## 8. 根拠サマリ
1. **複雑度の集中**: learning-workbook（CAS 同値判定）と curriculum-generation（多段検証パイプライン）が全体の複雑度を牽引（[論点-001/002] 未確定のため rough ±300%）。
2. **NFR は低スケール**: 個人ツール DAU〜100 + 事前生成キャッシュで NFR 倍率 ≈0.56x、実装ボリュームを圧縮。
3. **セキュリティ要件は実装に内包**: SEC-001〜004（認可/sanitize/PII/DSR）は各 feature の Std 行数に織り込み済み（別途上積みなし）。
4. **横断が先行投資**: 8 横断（4,350 行）が基盤。app-shell 合成レイヤは最後。

> **次の校正タイミング**: Phase 2 で最初の 1 feature 完了直後に `/flow:estimate` を再実行 → 実績（LOC/tokens/active_min）を取り込み `refined_<date>.md` 生成（confidence 収束）。以降の再校正は不要。
